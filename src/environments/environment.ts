// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  firebaseConfig : {
    apiKey: "AIzaSyCPE3XcmyGroQwaqezNxW6Xgn-GaGaGdrY",
    authDomain: "vsrikyweb.firebaseapp.com",
    projectId: "vsrikyweb",
    storageBucket: "vsrikyweb.appspot.com",
    messagingSenderId: "832206458210",
    appId: "1:832206458210:web:6ae76f943adec583d5f03b",
    measurementId: "G-CR5B56G5PG"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
